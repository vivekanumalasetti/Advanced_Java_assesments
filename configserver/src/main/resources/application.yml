server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vivekanumalasetti/Spring-Config-rep3
          clone-on-start: true
  application:
    name: config-service